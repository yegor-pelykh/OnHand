{
  "name": "on_hand_post_actions",
  "version": "1.0.0",
  "description": "Post actions for the OnHand project",
  "private": true,
  "scripts": {
    "sw-build": "tsc -p web_ts",
    "sw-minify": "esbuild web/service_worker.js --outfile=web/service_worker.js --bundle --minify --allow-overwrite",
    "sw-build-release": "npm run sw-build && npm run sw-minify",
    "pack": "node build_post_pack.js"
  },
  "author": {
    "name": "Yegor Pelykh",
    "email": "yegor.dev@gmail.com",
    "url": "https://github.com/yegor-pelykh"
  },
  "license": "MIT",
  "dependencies": {
    "@types/chrome": "latest",
    "archiver": "latest",
    "esbuild": "latest"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
